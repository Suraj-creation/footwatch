version: '3.8'

services:
  localstack:
    image: localstack/localstack:latest
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,sqs,dynamodb

  dynamodb-local:
    image: amazon/dynamodb-local:latest
    ports:
      - "8005:8000"
